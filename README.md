# Quiz da Onça 🐆

Pequeno site em **HTML/CSS/JS** que apresenta um quiz de 5 perguntas com tema de onça. Ao responder todas as perguntas o site libera a tela de comemoração com a mensagem **FELIZ ANIVERSÁRIO!** e confetes.

Como usar

1. Abra `index.html` no navegador (duplo clique ou arraste para o navegador).
2. Responda cada pergunta — algumas pedem que você digite a resposta (campo de texto), outras são escolhas. Use o botão **Verificar** nas perguntas de texto; quando estiver correto, a próxima pergunta será exibida.
3. Ao final, será exibida a tela de comemoração; use "Jogar de novo" para reiniciar.

Personalização rápida

- Para alterar as perguntas edite `script.js` no array `questions` (são 5 perguntas no total; aceite variações simples de resposta como "0" e "zero").
- O arquivo de rugido agora pode ser local: coloque-o em `assets/` e atualize `SOUND_URL` em `script.js` (uso atual: `./assets/571287__lewisbm__jaguar_growl_roar.wav`).
- Cores e tema no `styles.css`.
- O cartão final exibe uma imagem fixa presente em `assets/` (atualmente: `WhatsApp Image 2025-12-31 at 16.42.58.jpeg`).
- A imagem do banner foi substituída por foto de onça-pintada (fonte: Pexels).

Licença: livre para uso pessoal e adaptação.
